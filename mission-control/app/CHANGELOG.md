# Mission Control App Changelog

## v1 scaffold (GitHub-first)
- Added route map for core modules and sub-apps.
- Added typed domain models (`types.ts`).
- Added mock service layer (`services/mockData.ts`).
- Added contract loader/validator placeholder (`services/contracts.ts`).
- Added app README with migration steps to Vite React TS.

## Next
- Initialize actual React app and wire these files.
- Add Ajv validation + contract health UI.
- Connect real data adapters from trading/meta/ga4/gtm/gmail.
